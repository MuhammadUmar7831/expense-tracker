import express from "express";

const router = express.Router();

// router.get('/get/:budgetId', authenticate, getBudget);
// router.post('/add/:budgetId', authenticate, addBudget);
// router.put('/update/:budgetId', authenticate, updateBudget);
// router.delete('/delete/:budgetId', authenticate, deleteBudget);

export default router;
